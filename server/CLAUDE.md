# server/ — OUT OF SCOPE

This package contains the DCRDEX centralized exchange server and is **out of scope** for this project. Do not modify, extend, or add dependencies on code in this directory.

If you need server-side blockchain interaction patterns as a reference, read the code but do not port it here — find the equivalent in `wallet/asset/` instead.
